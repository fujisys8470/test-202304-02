進捗4/6終了時点

Javaの学習(「配列」まで完了)

JUnitの簡単なテストクラスを作成、テストの実行

SQL 「3 SELECT from Nobel」まで終了

理解したこと

for文が多重の場合break文とラベルを用いて指定したfor文から抜け出すことができる

配列のfillメソッドを用いることで配列の要素すべてに指定した値を代入することができる

配列のコピーに「shallow copy」と「deep copy」があること

shallow copy

配列の値を格納している位置をコピーするのでコピー元の値を変更するとコピー先の値も変化する

deep copy

コピー元の値そのものをコピーする

やること

「subject IN ('Physics','Chemistry')」について調べる(postgreSQLでは実行可能)